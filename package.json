{
  "name": "recoup",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "eval": "bun run src/eval/run.ts",
    "demo": "bun run src/demo/run.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
